# Citation fixture source packet

Mission: `ai-citation-integrity`

This mission uses team-created deterministic registry-like responses. It does
not claim that a live Crossref, OpenAlex or web search was performed.

Fixture source facts:

- DOI-shaped string: `10.4242/jamr.2025.0199`
- Fixture result: not found in queried deterministic registry packet
- Required learner-facing wording: not found in queried sources, not fabricated
- Use basis: team-created metadata-only training fixture

Limitations:

- The fixture teaches citation verification process. It is not a public claim
  about a real publication, journal or database result.
- Future live provider adapters require schema-reviewed contracts and outage
  tests before replacing the deterministic demo path.
