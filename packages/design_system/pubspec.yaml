name: design_system
description: Evidence Gym shared design tokens, theme and reusable Flutter components (Role 1 owned).
version: 0.1.0
publish_to: "none"

# See apps/learner/pubspec.yaml for why 3.29 is the floor.
environment:
  sdk: ">=3.7.0 <4.0.0"
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
